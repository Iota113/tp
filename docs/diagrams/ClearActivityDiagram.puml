@startuml
start
:User inputs clear command;
:Show confirmation dialog;
if () then ([User keys in 'y'])
    :Clear Contact List;
    :Show Success Message;
else ([User keys in 'n'])
    :Abort Clearing;
    :Show Aborted Message;
endif
stop
@enduml
